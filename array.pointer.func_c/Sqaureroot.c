#include<stdio.h>
#include<math.h>
int main()
{
      int a;
      printf("Enter the number: ");
      scanf("%d",&a);

      printf("Sqaure root number is: ");
      int root= sqrt(a);
      printf("%d",root);
}